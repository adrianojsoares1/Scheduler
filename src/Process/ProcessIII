class ProcessIII extends Process {

    public ProcessRunTime() {
        int runTime = 0;
        int num = RNG1.RNG_float();
        runTime = Math.ceil(150 + 350 * num.pow(5 / 2));

        setFinishTime(runTime);
    }
}